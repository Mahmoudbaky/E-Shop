"use client";

const ReviewForm = ({
  userId,
  prodcutId,
  onReviewSubmitted,
}: {
  userId: string;
  productId: string;
  onReviewSubmitted: () => void;
}) => {
  return <div>ReviewForm</div>;
};

export default ReviewForm;
